node_modules/
.next/
out/
.env
.env.local
.DS_Store
*.log
next-env.d.ts
.netlify/
dev.log

# npm run 목록 이 만드는 표. 파일에서 언제든 다시 뽑으므로 저장소에 두지 않는다.
목록.csv

# 빌드할 때 Cloudflare 에서 받아 만드는 인기글 순위. 배포마다 새로 만든다.
content/popular.json

# 글 폴더의 그림을 빌드할 때 여기로 옮겨 담는다. 원본은 content/posts 안에 있다.
# 사본을 함께 두면 어느 쪽이 원본인지 헷갈리고 한쪽만 고치게 된다.
/public/images/*
!/public/images/logo.png
!/public/images/brand-card.png
!/public/images/공통/
!/public/images/팀/

# 손으로 만든 임시 스크립트
/t*.mjs

# 타입 검사(tsc)가 남기는 찌꺼기
tsconfig.tsbuildinfo
작업/

# 파이썬 도구 부산물
__pycache__/
*.pyc
